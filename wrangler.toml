name = "justfortytwo-website"
compatibility_date = "2025-07-15"
# The build output directory is intentionally NOT pinned here — it's set in the
# Cloudflare Pages dashboard ("Build output directory" = `dist`). On Cloudflare,
# Nitro writes `dist/` (CF_PAGES auto-detection), not the local `.output/public`.
